package com.roomcraft.model;

/**
 * FurnitureItem model - one piece of furniture placed in a room
 * Fields: type (String), x (double), y (double),
 *         width (double), height (double), rotation (double),
 *         color (Color), shaded (boolean), objFilePath (String)
 * Methods: copy(), get3DHeight()
 * Assigned to: Member 2
 */
public class FurnitureItem {
    // TODO: implement fields, constructor, copy(), get3DHeight()
}
