package com.roomcraft.model;

/**
 * RoomConfig model - stores room dimensions and appearance
 * Fields: name (String), width (double), length (double), height (double),
 *         shape (String: Rectangle/L-Shape/U-Shape),
 *         wallColor (Color), floorColor (Color), theme (String)
 * Assigned to: Member 2
 */
public class RoomConfig {
    // TODO: implement fields and constructor
}
