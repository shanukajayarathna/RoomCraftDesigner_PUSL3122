package com.roomcraft.util;

/**
 * Password Hasher utility
 * Uses SHA-256 via java.security.MessageDigest (no external library needed)
 *
 * Methods:
 *   hash(String password)             -> String (hex SHA-256)
 *   verify(String plain, String hash) -> boolean
 *
 * Assigned to: Member 2
 */
public class PasswordHasher {
    // TODO: implement hash() and verify()
}
